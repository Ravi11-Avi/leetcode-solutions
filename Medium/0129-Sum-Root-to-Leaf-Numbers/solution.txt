// ═══════════════════════════════════════════════════════
//  Problem  : 0129. Sum Root to Leaf Numbers
//  URL      : https://leetcode.com/problems/sum-root-to-leaf-numbers/submissions/2091774300/
//  Difficulty : Medium
//  Language : chooseatype
//  Solved   : August 2, 2026
// ═══════════════════════════════════════════════════════

/**
 * Definition for a binary tree node.
 * public class TreeNode {
 *     int val;
 *     TreeNode left;
 *     TreeNode right;
 *     TreeNode() {}
 *     TreeNode(int val) { this.val = val; }
 *     TreeNode(int val, TreeNode left, TreeNode right) {
 *         this.val = val;
 *         this.left = left;
 *         this.right = right;
 *     }
 * }
 */
class Solution {
    public int sumNumbers(TreeNode root) {
        // int ans =  0;
        // if (root == null)return ans;
        return dfs(root ,0);
       
    }

    public static int dfs(TreeNode node ,   int currNum){
        if (node == null)return 0 ;

        currNum = currNum*10 + node.val;

        if (node.left ==  null && node.right == null ){
            return currNum;
        }  

        return dfs(node.left, currNum) +dfs(node.right, currNum);

    }

}