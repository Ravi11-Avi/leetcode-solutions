// ═══════════════════════════════════════════════════════
//  Problem  : 0938. Range Sum of BST
//  URL      : https://leetcode.com/problems/range-sum-of-bst/
//  Difficulty : Easy
//  Language : chooseatype
//  Solved   : August 3, 2026
// ═══════════════════════════════════════════════════════

/**
 * Definition for a binary tree node.
 * public class TreeNode {
 *     int val;
 *     TreeNode left;
 *     TreeNode right;
 *     TreeNode() {}
 *     TreeNode(int val) { this.val = val; }
 *     TreeNode(int val, TreeNode left, TreeNode right) {
 *         this.val = val;
 *         this.left = left;
 *         this.right = right;
 *     }
 * }
 */
class Solution {
    public int rangeSumBST(TreeNode root, int low, int high) {
        if (root == null) return 0;


        return dfs(root, low ,high , 0);
    }

    public static int dfs (TreeNode node , int low , int high , int total ){
        if(node== null)return total;
        
        if(node.val>= low && node.val<=high ){
            total+= node.val;
        }
        total = dfs(node.left,low,high, total);
        total = dfs(node.right,low,high, total);

        return total;
    }
}